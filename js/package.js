class Package {
    constructor(message, send, receive, cable, x, y, direction) {
        this.message = message;
        this.send = send;
        this.receive = receive;
        this.cable = cable;
        this.direction = direction;
        this.x = x;
        this.y = y;
    }
}
